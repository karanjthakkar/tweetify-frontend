import { autoLink } from '../../../helpers/auto-link';
import { module, test } from 'qunit';

module('Unit | Helper | auto link');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = autoLink(42);
  assert.ok(result);
});
