import { colorScore } from '../../../helpers/color-score';
import { module, test } from 'qunit';

module('Unit | Helper | color score');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = colorScore(42);
  assert.ok(result);
});
