import { numberHumanize } from '../../../helpers/number-humanize';
import { module, test } from 'qunit';

module('Unit | Helper | number humanize');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = numberHumanize(42);
  assert.ok(result);
});
